public class InventoryModel
{
    // Dictionary<string, int>

    // Add(string id, int amount)

    // bool TryRemove(string id, int amount)

    // int GetCount(string id)

    // bool Has(string id, int amount)
}